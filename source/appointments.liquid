---
pagination:
  data: level3array
  size: 1
  alias: service
  addAllPagesToCollections: true
permalink: "help/{{ service.key | slug }}/appointment/"
layout: layouts/iframes.liquid
tags: 
  - appointment
  - iframe
eleventyComputed:
  title: "{{ service.title }}"
---

<iframe
  title="Terminvereinbarung im Service-Portal Berlin"
  {% if service.appointment.berlin.url %}
    src="{{ service.appointment.berlin.url }}/"
  {% else %}
    src="about:blank"
  {% endif %}
  
></iframe>
